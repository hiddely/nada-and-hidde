{
  "name": "nada-and-hidde",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "serve . --listen 3000",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build": "node scripts/build.mjs",
    "preview": "serve dist --listen 3000"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "html-minifier-terser": "^7.2.0",
    "prettier": "^3.9.9",
    "serve": "^14.2.6",
    "sharp": "^0.35.4"
  }
}
